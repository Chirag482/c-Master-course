#include <iostream> // header file that includes input/output functions
using namespace std;  // it states that we are using standard namespace of c++
int main(){     //start
    cout<<"Hello Chirag"<<endl;     //endl is used to terminate the current line which means it includes a new line character in it i.e."\n"

    // double slash is used to add single line comments
    /* it is
    used to add mutiple
    line comments*/

    return 0;       //exit
}
